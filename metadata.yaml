# Copyright 2022 ubuntu
# See LICENSE file for licensing details.

# For a complete list of supported options, see:
# https://juju.is/docs/sdk/metadata-reference
name: charm-openidc-test-fixture
display-name: |
  Keycloak OIDC Test charm
description: |
  Keycloak OIDC Test charm
summary: |
  Keycloak OIDC Test charm

# TEMPLATE-TODO: each container defined above must specify an oci-image resource
resources:
  dockerimg:
    type: file
    filename: dockerimg.tar.gz
    description: |
      Tarball file for the oidc profider docker image.